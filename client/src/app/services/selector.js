// @flow
export default (state: Object, all: boolean = true, modules: Array<string> = []) => {
    if (all) {
        return {
        };
    }

    let stateInProps = {};


    return stateInProps;
};
